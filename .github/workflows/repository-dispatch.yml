name: Test-repository-dispatch-event
on:
  repository_dispatch:
    types: [on-demand-test]

jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🐧 This job is on running example branch ${{ toJson(github.event.client_payload) }}"
